use std::process::Command;

use std::io;
use std::io::prelude::*;

struct FileName {
    filename: String,
    fileextension: String,
    path: String,
}

impl FileName {
    fn new(fn_inp: String) -> FileName {
        FileName {

            filename,
            fileextension,
            path,
        }
    }
}


// fn read_file_bytes(str::)


fn main() {


}